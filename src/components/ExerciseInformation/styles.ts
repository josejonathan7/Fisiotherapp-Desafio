import { StyleSheet } from "react-native";
import { theme } from "../../theme";

export const styles = StyleSheet.create({
	container: {
		alignItems: "center"
	},
	title: {
		fontSize: 18,
		color: "#A29697"
	},
	information: {
		fontSize: 16,
		color: theme.COLORS.lighterPurple
	}
});
